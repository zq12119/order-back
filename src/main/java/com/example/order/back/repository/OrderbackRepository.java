package com.example.order.back.repository;

import com.example.order.back.entity.Orderback;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderbackRepository extends JpaRepository<Orderback,Integer> {
}
